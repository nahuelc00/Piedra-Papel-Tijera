{
  "name": "desafio-modulo-5",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "PORT=8080 parcel serve ./src/index.html",
    "build": "parcel build ./src/index.html --dist-dir build --public-url /Piedra-Papel-Tijera",
    "deploy": "gh-pages -d build",
    "build-and-deploy": "npm run build && npm run deploy"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.2.1",
    "typescript": "^4.6.0-dev.20220128"
  },
  "dependencies": {
    "gh-pages": "^3.2.3"
  }
}
